---
title: "New workflow"
description: "Create a new durable automation from a blank canvas."
---

<Frame>
  <img className="block dark:hidden" src="/images/light/workflow-new.png" alt="New workflow editor in light theme" />
  <img className="hidden dark:block" src="/images/dark/workflow-new.png" alt="New workflow editor in dark theme" />
</Frame>

The new workflow page at `/workflows/new` opens the workflow editor on an empty canvas so you can author an automation from scratch.

## Using the page

- Add a **trigger**, then drag **steps** onto the canvas and connect their typed handles.
- Configure step settings, then **Validate** to check the graph.
- **Save draft** to keep working, or **Publish** to create the first immutable version.

## Notes

- A trigger is required and every step must be reachable.
- See [Workflow editor](/workflows/workflow-edit) for the full set of step and validation options.
